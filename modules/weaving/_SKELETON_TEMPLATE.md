# 混合型交织矩阵骨架模板

> 版本：0.3.0 | 状态：骨架定义（完整交织指令推迟至 v1.0.0）
> 使用方式：当触发此混合对时，Agent 使用"主导+补充"并行模式（v0.2.1行为），同时加载此骨架作为参考框架。

---

## 交织概览（骨架）

```
主导流水线：{{PRIMARY}}（主导类型完整执行）
补充模块：{{SECONDARY}}（补充类型专家注入）
当前策略：v0.2.1 并行模式 + 骨架框架参考
```

## Step A 交叉维度识别（骨架）

> Agent 应在 Step A 输出时手动标注交叉维度。以下为建议交叉点：

| {{PRIMARY}} 维度 | {{SECONDARY}} 维度 | 潜在交叉 |
|------------------|-------------------|---------|
| [待 v1.0.0 填充] | [待 v1.0.0 填充] | [待 v1.0.0 填充] |

## Step B 联合评审建议（骨架）

> Agent 应在 Step B 评审序列中，在主导类型专家评审完成后，补充类型专家针对交叉议题给出独立意见。

## Step C 依赖排序（骨架）

> Agent 应在任务清单中标注跨类型依赖（@DEP_ORDER 指令），但 v0.3.0 中仅作提示，不强制执行。

## Step D 联合门禁（骨架）

> Agent 应在 Step D 出口同时执行两个类型的质量门禁，但不要求联合判定（各自独立通过即可）。

## Step E 合并交付（骨架）

> Agent 应将两个类型的交付物清单合并，但不要求交叉引用（各自独立交付）。

---

> **v1.0.0 升级路径**：填充本文件所有 `[待 v1.0.0 填充]` 标记，升级为完整交织矩阵。
